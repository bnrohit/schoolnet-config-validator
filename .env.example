# SchoolNet Config Validator environment
APP_ENV=production
API_PORT=8000
WEB_PORT=3000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Keep live SSH disabled until you are on a trusted internal network.
ENABLE_LIVE_SSH=false
MAX_UPLOAD_SIZE_MB=2
